<form action="/restaurants/{{restaurant._id}}/edit" method="POST">
    <input type="text" placeholder="new name" name="name" value="{{restaurant.name}}">
    <button type="submit">Save</button>
    <a href="/">Back</a>
</form>
